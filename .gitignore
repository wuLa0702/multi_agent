# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ── 环境配置（密钥不入库；.env.example 保留）──
.env
.env.dev
.env.prod
.env.local

# ── 数据/日志 ──
data/
logs/
*.db
*.sqlite3

# ── IDE / 系统 ──
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# ── Clowder AI harness 运行时（挂载/生成物，可再生，不入库）──
.cat-cafe/
.claude/hooks/
.claude/skills/
.codex/
.gemini/
.kimi/

# ── 运行时产物（可再生，不入库）──
.playwright-mcp/          # 浏览器实测产物（page 快照 / console log / 截图）
skill-resources/          # 静态技能目录（paths.py get_static_skills_dir 自动重建 README）

# ── 前端 ──
frontend/node_modules/
frontend/dist/
/前端ui模仿图片
