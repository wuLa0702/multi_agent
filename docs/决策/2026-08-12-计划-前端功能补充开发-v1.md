# 计划-前端功能补充开发 v1（F1-F6 实施 · 逐项更新）

> 📋 **规范**：遵循 `docs/规则/文档规范.md`；前置设计：`决策/2026-08-12-设计-前端功能补充详设-v1.md`
> 📌 **更新时间**：2026-08-12
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1 | 2026-08-12 | 初版：F1-F6 实施清单 + 执行日志（防中断）；mock 先行 → 前端单测 → 前后端联调 |

> **目录**：
> - §1 目标与策略
> - §2 任务清单（逐项状态）
> - §3 执行日志（做一项记一项）

---

## 1. 目标与策略 🔴 v1

**目标**：按详设实现 F1-F6（模型单价/成本展示/cost_alert/MCP 管理/设置中心/文件引用），前端单测覆盖，再前后端联调。

**策略**：
1. **mock 先行**：`mock.ts` 补 REST mock（providers 单价/cost/mcp/settings），`client.ts` 新方法加 `MOCK_ENABLED` 分支——前端可脱离后端独立跑
2. **逐项实现 + 单测**：每项 commit + 对应 vitest 单测，防回归
3. **联调**：`MOCK_ENABLED=false` + 起后端 8010 + 前端 dev 5176，实测 F1-F6 数据打通，修数据/契约问题

**基建（实证）**：vitest + jsdom（`vite.config.ts` test 段）；`src/test/{setup,chatStore,sse}.test.ts`；`MOCK_ENABLED=true`（mock.ts:12）；client.ts 已有 mcp/settings 方法、缺 price/cost。

---

## 2. 任务清单（逐项状态） 🔴 v1

> 状态：⬜ 待做 ｜ 🟦 进行中 ｜ ✅ 完成

| # | 任务 | 落点 | 状态 |
|---|------|------|------|
| F1 | 模型单价：`api.updateModelPrice` + mock + `ModelPriceList` 组件 + SettingsPage 接线 | `lib/api/{client,types,mock}.ts` + `components/settings/ModelPriceList.tsx` + `pages/SettingsPage.tsx` | ⬜ |
| F2 | 成本展示：`api.getCostSummary/getCostAlerts` + mock + `CostPanel` 组件 + ChatPage 接线 | 同上 + `components/chat/CostPanel.tsx` + `pages/ChatPage.tsx` | ⬜ |
| F3 | cost_alert：types 加分支 + chatStore case + mock 流补事件 + 单测 | `lib/api/{types,mock}.ts` + `lib/stores/chatStore.ts` + `src/test/chatStore.test.ts` | ⬜ |
| F4 | MCP 管理：`McpServerPanel` 组件（client 已有方法）+ SkillsPage Tab + mock | `components/skills/McpServerPanel.tsx` + `pages/SkillsPage.tsx` + mock | ⬜ |
| F5 | 设置中心：SettingsPage 接 settings API + 热更新提示（client 已有方法）+ mock | `pages/SettingsPage.tsx` + mock | ⬜ |
| F6 | 文件引用：`FileRefChip` 组件 + MessageBubble 渲染 FileRef + types | `components/chat/FileRefChip.tsx` + `components/chat/MessageBubble.tsx` + types | ⬜ |
| 全测 | 前端全量单测绿（49 + 新增）| `pnpm test` | ⬜ |
| 联调 | `MOCK_ENABLED=false` + 后端 8010 + 前端 5176，F1-F6 数据打通 | 起服务实测 | ⬜ |

---

## 3. 执行日志（做一项记一项） 🔴 v1

| 时间 | 任务 | 动作 | 结果 |
|------|------|------|------|
| 2026-08-12 | 基建 | pnpm install（node_modules 缺失）；vitest 配置确认 | 安装中 |
