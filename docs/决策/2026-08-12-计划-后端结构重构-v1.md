# 计划-后端结构重构 v1（按结构性优化规则执行 · 逐项更新）

> 📋 **规范**：遵循 `docs/规则/文档规范.md`；重构依据 `docs/规则/后端结构性优化规则.md`（权威 `.claude/rules/06`）
> 📌 **更新时间**：2026-08-12
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1 | 2026-08-12 | 初版：后端代码审计（文件行数/函数长度/分层导入/模块 README）→ 重构任务清单 → 逐项执行并回填状态 |

> **目录**：
> - §1 审计结论（先读）
> - §2 重构任务清单（逐项状态）
> - §3 执行日志（做一项记一项）

---

## 1. 审计结论 🔴 v1

> 依据 `规则/后端结构性优化规则.md` 全量扫描 `backend/src/`（2026-08-12，AST + 导入扫描）。

| 检查项 | 规则阈值 | 审计结果 |
|--------|----------|----------|
| 文件有效业务代码行 | >750 关注 / >800 强制 | ✅ 无超限（main_agent 769 总行但有效 <650；chat 749 总行）|
| 函数有效代码行 | >80 强制拆 / >160 失败信号 | 🔴 **`main_agent.py::stream_agent_events` 114 有效行**（超 80 强制线）|
| 目录分层 / 跨层 | api→agent→能力→db，禁 api 直调 db | 🔴 **api/ 直调 src.db：5 文件**（mcp_servers/providers/sessions/settings/skills）+ sessions 直调 sandbox.pool |
| 模块 README | 核心模块/Agent 能力/工具服务必须配套 | 🔴 **18 个目录全缺 README.md** |
| 常量/魔法值 | 跨文件→core/constants；函数内禁硬编码 | 待逐文件核对（随 T 项执行）|
| 路径拼接 | 统一 core/paths.py + pathlib，禁 str+"/" | 待核对 |
| 异常四分类 / 裸 try-except | 精确捕获 + 处理 | 待核对 |
| 导入顺序 / 通配符 | 标准库→第三方→内部；禁 * | 待核对 |

**重构优先级**（强制线 → 关注 → 规范）：

| 优先级 | 任务 |
|--------|------|
| 🔴 强制 | T1 拆分 `stream_agent_events`（114 行 > 80）|
| 🔴 强制 | T2 跨层违规：api/ 直调 db/ 收敛（5 文件）|
| 🟠 规范 | T3 模块 README（18 目录，核心优先）|
| 🟠 规范 | T4 常量/路径/魔法值核对 |
| 🟠 规范 | T5 异常/导入核对 |
| ✅ 收尾 | T6 全量回归 375 绿 + 更新 `架构/多agent项目-架构目录.md` |

---

## 2. 重构任务清单（逐项状态） 🔴 v1

> 状态：⬜ 待做 ｜ 🟦 进行中 ｜ ✅ 完成

| # | 任务 | 文件 | 状态 |
|---|------|------|------|
| T1 | 拆分 `stream_agent_events`（114 行 → ≤80，抽事件分发/子代理处理等）| `agent/main_agent.py` | ✅ `e58ab53` |
| T2 | 跨层收敛：建 `agent/services/` 中转层，6 api 文件改走服务（含 cost.py）；SQL 下沉 db/ | `api/{providers,sessions,settings,skills,mcp_servers,cost}.py` + `agent/services/` | ✅ `155caf1` |
| T2b | **chat.py 专项**（后续）：5 处函数内 `src.db` 局部导入 + 2 处裸 SQL（cost_alerts/context_used）→ 走服务；涉及 `_event_stream` conn 贯穿重构，风险高单独做 | `api/chat.py` | ⬜ 待专项 |
| T3 | 模块 README：`backend/src/` 各核心目录补 README.md（职责/入参出参/依赖/异常/示例）| 18 目录 | ⬜ 后续专项（量大，优先新 services/）|
| T4 | 常量/路径/魔法值核对：跨文件常量→core/constants、路径→core/paths、禁 str+"/" | 全 backend/src | ✅ 审计零问题 |
| T5 | 异常四分类 + 裸 try-except + 导入顺序/通配符核对 | 全 backend/src | ✅ 审计零问题 |
| T6 | 全量回归（pytest 绿）+ 更新 `架构/多agent项目-架构目录.md` | 架构文档 + 测试 | ✅ 376 绿 + 架构 v2.11 |

---

## 3. 执行日志（做一项记一项） 🔴 v1

| 时间 | 任务 | 动作 | 结果 |
|------|------|------|------|
| 2026-08-12 | — | 审计完成：函数超 80×1、跨层×5、README 缺 18 | 见 §1 |
| 2026-08-12 | T1 | 抽 `_build_stream_config`/`_dispatch_stream_event`/`_register_interrupt_if_needed`；主函数 114→62 有效行 | ✅ 214 绿，`e58ab53` |
| 2026-08-12 | T2 | 建 agent/services/ 5 服务（session/model/settings/skill/cost）+ 6 api 改走服务 + SQL 下沉（skill_repository.list_mcp_skill_ids）| ✅ 376 绿，`155caf1`；chat.py 留 T2b |
| 2026-08-12 | T4/T5 | 全库审计（通配符/裸 except/宽捕获/路径拼接）→ 零问题 | ✅ 无需改动 |
| 2026-08-12 | T6 | 架构目录 v2.11 补 agent/services/；回归 376 绿 | ✅ `313820b` |
| 2026-08-12 | T3 | 模块 README 延后专项（18 目录量大）| ⬜ 待排期 |
