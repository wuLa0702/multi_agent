# 计划-前端E2E测试 v1（develop 分支真实浏览器实测 · 执行中动态更新）

> 📋 **规范**：遵循 `docs/规则/文档规范.md`
> 📌 **更新时间**：2026-08-06
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动（精确到二级标题） |
> |------|------|------|
> | v1 | 2026-08-06 | 初版：develop 合并后端迁移 + 前端 B1-B6 后，真实浏览器（playwright）E2E 测试计划——15 用例 × 步骤/预期；按计划推进、逐项勾选、动态更新；测试完成后转测试报告 |

> **目录**：
> - §0 测试目标与范围
> - §1 测试环境
> - §2 用例清单（执行状态动态更新）
> - §3 执行记录

---

## 0. 测试目标与范围

- **目标**：验证 develop 分支（后端目录迁移 + 前端 B1-B6）在真实浏览器中的端到端表现，重点覆盖新增功能（一键到底/审批 UI/子代理面板/任务规划面板/HITL 开关/模式切换）与核心回归（对话流/历史/模型/设置）
- **范围**：前端 UI 全链路（mock 假数据源，`MOCK_ENABLED=true`）；后端接口不在本次范围（mock 已按契约对齐）
- **环境版本**：develop @ `a5a5ed6`（+f3673b6 清理）

## 1. 测试环境

| 项 | 值 |
|----|-----|
| 前端 | vite dev server `http://localhost:5180`（develop 工作区） |
| 数据源 | mock 流（MOCK_ENABLED=true，契约对齐） |
| 浏览器 | playwright 真实浏览器（chrome） |
| 预置 | localStorage 清理（pendingRunId/uiMode 等残留） |

## 2. 用例清单（状态：⏳ 待测 / 🔄 执行中 / ✅ 通过 / ❌ 失败）

| # | 用例 | 步骤 | 预期 | 状态 |
|---|------|------|------|------|
| E2E-01 | 页面加载与布局 | 打开 5180；导航 对话/能力市场/设置 | 三页面可切换；四栏布局正常；控制台无未捕获错误 | ✅ |
| E2E-02 | 对话流回归 | 输入消息发送 | 用户消息入流 → token 流式渲染 → 工具调用块展示 | ✅ |
| E2E-03 | 一键到底（B1） | 发送长消息后向上滚动脱离底部 500px | 「回到最新」按钮出现（含未读计数）→ 点击平滑回底 → 按钮隐藏 | ✅ |
| E2E-04 | 子代理模式（B3） | 发送消息触发 subagent 事件 | SubagentPanel 内嵌面板：进度条 + 卡片（运行中脉冲→完成勾）+ 可折叠 | ✅ |
| E2E-05 | 任务规划模式（B4） | 发送消息触发 todos 事件 | TodoPanel 顶部固定：5 步任务逐步完成、进度百分比、折叠 | ✅ |
| E2E-06 | 审批-批准（B2） | 发送消息触发 approve → 点「批准」 | 审批卡片（高危徽标/参数/三决策）→ 批准 → 恢复流 token → 历史行「已批准」 | ✅ |
| E2E-07 | 审批-拒绝 | 同上 → 输入理由点「拒绝」 | 历史行「已拒绝 · 理由可见」 | ✅ |
| E2E-08 | 审批-编辑后批准 | 同上 → 「编辑后批准」→ 改 JSON → 确认 | 编辑区出现 → 确认提交 → 恢复流 | ✅ |
| E2E-09 | HITL 开关联动（B5） | 设置页关闭 HITL → 回对话发送 | 无审批卡片；消息含「HITL 已关闭」自动执行；开关持久化 | ✅ |
| E2E-10 | 模式切换（B6） | 标题栏「视图模式」下拉手动切换；另验证自动升级 | 四模式下拉可选、当前勾选；subagent/todos 自动升级模式；手动优先 | ✅ |
| E2E-11 | 模型选择回归 | 输入栏「选择模型」下拉 | 厂商→模型二级联动展示 | ✅ |
| E2E-12 | 历史会话回归 | 会话列表切换/新建 | 列表渲染、新对话清空 | ✅ |
| E2E-13 | 上下文用量（B5） | 完成一轮对话后查看用量 | done 后进度条显示真实值（非 0 占位） | ✅ |
| E2E-14 | 技能市场回归 | 切到能力市场页 | 页面加载、分类/搜索 UI 正常 | ✅ |
| E2E-15 | 设置页回归 | 打开设置页各菜单 | 账户/外观/系统配置渲染；HITL 开关可见 | ✅ |

## 3. 执行记录（每项：时间/结果/备注）

| 时间 | 动作 | 结果 |
|------|------|------|
| 12:31-12:36 | E2E-01/02/14：三页面导航 + 对话流 + 技能市场 | ✅ |
| 12:36-12:43 | E2E-04/05/06：子代理面板 + 任务面板 + 审批卡片 + 批准恢复流 + 历史行 | ✅ |
| 12:41 | E2E-07/08：拒绝理由 + 编辑后批准（历史 3 条齐） | ✅ |
| 12:43 | E2E-03：脱离 938px 按钮出现 → 点击回底隐藏（窗口缩放法） | ✅ |
| 12:52-12:56 | E2E-09/15：HITL 开关关闭 → 高风险自动执行；E2E-11 模型二级联动 | ✅ |
| 12:56 | E2E-10：四模式下拉 + ✓标记 + 自动升级 + 手动切换 | ✅ |
| 13:02-13:03 | E2E-12：更多操作 → 清空对话 → 空状态 | ✅ |
| 13:04 | E2E-13：修复后用量 8.6k/128k 真实显示 | ✅ |
| 全程 | 发现并修复：mock 模式 done 后 refreshMessages 清空消息（e9bba09）；ContextUsage API 覆盖 store（5e07e7c 优先 store + 13:04 store 空才查 API） | ✅ 已修复 |

**结论：15/15 全部通过**。发现 3 个 mock 适配 bug（refreshMessages 清空 / ContextUsage 覆盖 / HMR 陈旧代码），全部修复并回归 48 用例全绿。
