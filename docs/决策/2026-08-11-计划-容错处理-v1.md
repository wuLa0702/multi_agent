# 计划-容错处理 v1（异常四分类 + 工具重试 + 降级矩阵落地 · P1 面试加固）

> 📋 **规范**：遵循 `docs/规则/文档规范.md`（含 §4.4 核心代码章节、§4.6 面试要点要求）
> 📌 **更新时间**：2026-08-11
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1.2 | 2026-08-11 | 评审补充 3 项：§5.3 run_skill_script 移出重试（脚本有写操作实证——build_report.py 写 report_draft.md，重试幂等风险）+ 幂等性假设说明；§5.2 去 ConnectTimeout/ReadTimeout 冗余（TimeoutException 子类）；§5.4 MemorySession 降级改为复用传入 session 对象（项目无此类，免新建） |
> | v1.1 | 2026-08-11 | 评审修正 6 项：§5.2 retry.py 重写（sync/async 双版本 + extra_exceptions + httpx 内置 + import 补全）；§3.1 异常分类加边界示例；§3.3 会话降级行为明确；§3.2/5.3 MCP 范围明确（本地工具，远程 P2）；§4.3 tool_retry 日志格式；§6.1 补 AST 扫描 + 超时重试关系 |
> | v1 | 2026-08-11 | 初版：§1 背景与目标；§2 现状盘点（已有容错资产 vs 缺口）；§3 设计（异常四分类扩展 / 工具重试装饰器 / chat 降级矩阵落地 / 超时统一）；§4 面试要点（§4.6 三要求）；§5 完整核心代码（可运行形态）；§6 实施计划 |

> **目录**：
> - §1 背景与目标
> - §2 现状盘点（已有 vs 缺口）
> - §3 设计
> - §4 面试要点（面试点标注 / 追问链预演 / 可量化结果）
> - §5 完整核心代码（errors 四分类 + 重试装饰器 + 降级矩阵代码形态）
> - §6 实施计划与评审点

> **关联文档**：面试备战指南 §3.2（容错处理 P1）、后端功能全景 §8（待办）、结构优化规范 v2 §7（异常四分类 + 降级矩阵设计稿）、core/errors.py、llm/adapter.py

---

## 1. 背景与目标 ⚪ v1

**Why（双动机）**：
1. **面试**（备战指南 §3.2）：容错处理（P1）——"工程能力的体现"，追问点：工具调用失败重试、降级策略、超时处理、异常兜底。当前项目容错**散落不系统**，面试答"怎么容错"缺完整故事
2. **工程**：生产环境工具调用失败是常态（网络/限流/超时），无系统化重试+降级 = 对话中断率高、故障难定位

**目标**：把结构优化规范 v2 §7 的**异常四分类 + chat 链路降级矩阵**从设计稿落地为代码——统一异常体系、工具失败重试、链路逐节点降级、超时统一。

**边界（不做）**：不做熔断器（circuit breaker，单机单用户不需要）；不做分布式重试队列（单进程够用）。

---

## 2. 现状盘点（已有 vs 缺口） ⚪ v1

### 2.1 已有容错资产

| 位置 | 已有 | 覆盖 |
|------|------|------|
| `llm/adapter.py` | LLM 调用 timeout + max_retries 固化 | ✅ LLM 层 |
| `core/errors.py` | AppError 基类 + Retryable/NonRetryable/Config | ✅ 分级雏形（**两分类**）|
| 工具实现 | 错误返回字符串降级（不中断 run）| ✅ 工具层 |
| `chat.py` | 记忆/标题后台旁路降级（失败不阻断）| ✅ 旁路 |
| `eval_harness.py` | 任务失败重试 1 次 | ✅ 评估 |
| 结构优化规范 v2 §7 | 异常四分类 + 降级矩阵（**设计稿**）| ⚠️ 未落地 |

### 2.2 缺口（本计划落地）

| # | 缺口 | 现状 | 后果 |
|---|------|------|------|
| 1 | **异常四分类未落地** | errors.py 只有可重试/不可重试两分类 | 业务/系统异常无法区分，降级策略无法精准匹配 |
| 2 | **工具调用失败无重试** | 工具异常直接降级返回 | 瞬时故障（限流/超时）不重试，本可成功 |
| 3 | **chat 降级矩阵未逐节点核对** | 部分 try/except 裸捕获 | 单点失败可能断流/掩盖故障 |
| 4 | **超时不统一** | LLM 有，工具/搜索/沙箱各自 | 长挂起无兜底 |

---

## 3. 设计 🟠 v1.1

### 3.1 异常四分类扩展（core/errors.py）

| 分类 | 基类 | 定义 | 处理 | 边界示例（v1.1 评审明确） |
|------|------|------|------|--------------------------|
| 可重试 | `RetryableError`（已有）| 网络超时/抖动/限流 | 重试 N 次 → 仍失败降级 | 博查 429 限流、httpx 连接超时、沙箱临时不可达 |
| 不可重试 | `NonRetryableError`（已有）| 参数错/权限拒绝/校验失败 | 直接返回错误码 | 用户输入参数格式错、API key 无效、文件不存在、路径越权 |
| 业务 | `BusinessError`（新增）| 业务规则不满足 | 业务错误码 + 降级返回/默认兜底 | 研报字数超限、每日调用次数用完、内容审核不通过 |
| 系统 | `SystemError`（新增）| 未知内部错误 | 记 error 日志 + 按链路矩阵降级 | 未预期异常、依赖模块崩溃 |

> **边界判定口诀（v1.1）**：输入问题→不可重试；规则限制→业务；瞬时故障→可重试；其它未知→系统。`BusinessError` 与 `NonRetryableError` 区分：**前者是业务规则约束（改了输入也不一定过），后者是输入/权限本身非法（改对输入就过）**——例如"调用次数用完"（业务，等额度）vs "参数格式错"（不可重试，改参数）。

**与既有兼容**：`RetryableError`/`NonRetryableError` 不动，新增两个子类，基类加 `category` 字段（retryable/non-retryable/business/system）便于矩阵匹配。

### 3.2 工具调用失败重试（agent/middlewares/tool_retry.py 或 core/retry.py）

- **装饰器 `@retry_tool(retries=2, delay=0.5, only=(RetryableError, TimeoutError))`**：
  - 工具实现抛 `RetryableError`/超时 → 重试最多 2 次（退避 0.5s/1s）
  - 非可重试异常/业务异常 → 不重试直接降级
  - 重试耗尽 → 降级返回（错误字符串，不中断 run，与现状一致）
- **接入**：关键工具（internet_search / fetch_url / run_skill_script）包一层；或注册表统一包装（TOOL_REGISTRY 装配时加）
- **范围（v1.1 明确）**：本计划只覆盖**本地工具**（注册表内实现）；**MCP 远程工具**（外部 server）重试属 P2——在 mcp/client 调用层统一做（本地装饰器包装不了走 MCP 协议的工具）

### 3.3 chat 链路降级矩阵落地（结构优化规范 v2 §7.2 逐节点核对）

| 链路节点 | 失败形态 | 降级策略 | 断流？ | 落地动作 |
|----------|----------|----------|--------|----------|
| 会话/消息落库 | SQLite 错（可重试）| 重试 1 次 → 降级内存会话（**仅本次请求降级，后续消息继续尝试落库**；失败消息记日志告警——v1.1 明确）| 否 | chat.py 精确捕获 RetryableError |
| 记忆注入/读取 | Store 不可用 | 静默降级（跳过记忆）| 否 | 已有，核对 |
| 记忆写入 | Store 不可用 | 记 error + 队列重试（P2）| 否 | 已有旁路 |
| LLM 调用 | 超时/不可用 | 重试耗尽 → SSE error（retryable=true）| 是（协议设计）| 已有 |
| 工具调用 | 工具异常 | **本计划 §3.2 重试 → 错误结果回传** | 否 | 新增重试 |
| 子代理 | 子图异常 | 错误摘要回传主 agent | 否 | 核对 |
| 沙箱执行 | 沙箱不可用 | 返回工具错误 + AUTH/ENV 提示 | 否 | 已有降级 |
| 审核/审批 | checkpointer 异常 | 拒绝审批请求（400），状态保持 | 否 | 已有 |

**核对目标**：全链路无裸 `except Exception`（结构优化规范 §7.1 禁令），每节点精确捕获 + 降级。

### 3.4 超时统一

- 工具级：fetch_url 已有 10s；internet_search / run_skill_script 补统一超时（settings 可配 `tool_timeout_seconds`）
- 沙箱命令级：adapter.run_command 已有 timeout 参数（None = 不强制）——默认给 60s

---

## 4. 面试要点（文档规范 §4.6 三要求） 🟠 v1.1

### 4.1 面试点标注（对照备战指南 §3.2 五大模块）

| 面试点 | 本设计覆盖 | 技术故事 |
|--------|-----------|----------|
| **容错处理**（P1）| ✅ 全覆盖 | 异常四分类 + 工具重试 + 链路降级矩阵 |
| 成本控制（P1）| 部分（重试次数限制 = 成本护栏）| 重试预算 |
| 安全机制（P2）| 部分（降级不泄露内部错误细节）| 系统异常记日志不暴露 |

### 4.2 追问链预演（§3.3 五层自答）

| 追问 | 预演答案 |
|------|----------|
| **为什么做容错？** | 生产工具调用失败是常态（网络/限流/超时）——无系统化重试+降级 = 对话中断率高；面试工程能力体现 |
| **为什么异常四分类？** | 可重试（瞬时故障→重试）/不可重试（参数错→直接返回）/业务（规则不满足→降级兜底）/系统（未知→记日志不暴露）——降级策略精准匹配异常类型，避免"一刀切重试"浪费成本 或 "一刀切失败"丢失可恢复请求 |
| **工具失败为什么重试？** | 瞬时故障（限流 429/超时）本可成功——重试 2 次退避，成功率提升；重试预算（次数+退避）防雪崩 |
| **为什么不中断对话？** | 降级矩阵原则：除 LLM 主链路外全部"失败降级不断流"——工具失败回传错误让 Agent 自行处理，用户感知降级而非崩溃 |
| **裸捕获为什么禁？** | `except Exception` 包整段 = 掩盖真实故障（调试黑洞）——精确捕获 + 记录 + 降级三件套 |
| **怎么量化？** | 评估跑数统计：工具失败率 / 重试成功率 / 降级触发次数（trace 已采集工具调用，加异常计数）|

### 4.3 可量化结果（落地后预期产出）

- 工具失败率：评估跑数前后对比（重试后成功率 ↑）
- 重试命中次数：**tool_retry 采集格式（v1.1 明确）**——重试装饰器在每次重试打 `logging.info("工具 %s 重试 %d/%d（%s）", name, attempt, retries, exc_type)`（§5.2 已有）；评估/生产按 `grep "重试"` 或 trace 加 `tool_retry` 事件统计命中数与重试成功率
- 降级矩阵核对清单：全链路无裸捕获（**AST 扫描脚本 `scripts/check_no_bare_except.py`——v1.1 补：检测 `except Exception` 包整段业务代码的裸捕获，进校验工具 §13 同族**）

---

## 5. 完整核心代码（可运行形态） 🔴 v1.2

> 按文档规范 §4.4：给可运行代码形态。核心逻辑（异常分类/重试装饰器）手写，样板 AI 提速。

### 5.1 `backend/src/core/errors.py` 四分类扩展（增量）

```python
class AppError(Exception):
    """应用异常基类（统一携带 message + code + category）。"""

    def __init__(self, message: str, code: str = "APP_ERROR", *, category: str = "system") -> None:
        super().__init__(message)
        self.message = message
        self.code = code
        self.category = category  # retryable / non-retryable / business / system


class RetryableError(AppError):
    """可重试异常：网络超时、连接抖动、临时故障。"""

    def __init__(self, message: str, code: str = "RETRYABLE") -> None:
        super().__init__(message, code, category="retryable")


class NonRetryableError(AppError):
    """不可重试异常：参数错误、权限拒绝、业务校验失败。"""

    def __init__(self, message: str, code: str = "BAD_REQUEST") -> None:
        super().__init__(message, code, category="non-retryable")


class BusinessError(AppError):
    """业务异常：业务规则不满足 → 降级返回/默认兜底数据。"""

    def __init__(self, message: str, code: str = "BUSINESS") -> None:
        super().__init__(message, code, category="business")


class SystemError(AppError):
    """系统异常：未知内部错误 → 记 error 日志，按链路矩阵降级（不暴露细节）。"""

    def __init__(self, message: str, code: str = "INTERNAL") -> None:
        super().__init__(message, code, category="system")


class ConfigError(NonRetryableError):
    """配置缺失或非法。"""

    def __init__(self, message: str) -> None:
        super().__init__(message, code="CONFIG_ERROR")
```

> ⚠️ 兼容：现有代码 `retryable` 布尔属性保留（`@property` 映射 category == "retryable"），旧调用不破。

### 5.2 `backend/src/core/retry.py` 工具重试装饰器（v1.1 重写：sync/async 双版本）

```python
"""工具调用重试（P1 容错：瞬时故障重试，重试耗尽降级返回）。

原则（结构优化规范 v2 §7.1）：
- 只重试可重试异常（RetryableError / TimeoutError / httpx 网络异常 / 调用方 extra_exceptions）
- 业务/不可重试/系统异常不重试，直接降级
- 重试耗尽 → 返回错误提示字符串（与工具降级现状一致，不中断 run）
- v1.1 评审修正：自动检测 sync/async（asyncio.sleep 不阻塞事件循环）；
  内置 httpx 网络异常（httpx.TimeoutException/ConnectError/ReadError）；
  支持 extra_exceptions 参数（调用方指定额外可重试异常）
"""

import asyncio
import logging
import time
from collections.abc import Callable
from functools import wraps

import httpx

from src.core.errors import RetryableError

logger = logging.getLogger(__name__)

DEFAULT_RETRIES = 2
DEFAULT_DELAYS = (0.5, 1.0)  # 退避：0.5s / 1s

# 内置可重试网络异常（v1.1：httpx 库抛的不会被 RetryableError 捕获）
_NETWORK_EXCEPTIONS = (
    httpx.TimeoutException,  # 含 ConnectTimeout/ReadTimeout（v1.2 去冗余：父类即捕获子类）
    httpx.ConnectError,
    httpx.ReadError,
    TimeoutError,
)


def retry_tool(
    retries: int = DEFAULT_RETRIES,
    delays: tuple[float, ...] = DEFAULT_DELAYS,
    extra_exceptions: tuple[type[Exception], ...] = (),
) -> Callable:
    """工具函数重试装饰器（自动检测 sync/async，两种形态分别处理）。

    Args:
        retries: 最大重试次数
        delays: 每次重试的退避秒数
        extra_exceptions: 调用方额外指定的可重试异常类型（方案 C）

    Returns:
        包装后的工具函数（重试耗尽仍失败 → 降级错误字符串）
    """
    retryable = _NETWORK_EXCEPTIONS + extra_exceptions

    def decorator(func: Callable) -> Callable:
        if asyncio.iscoroutinefunction(func):
            @wraps(func)
            async def async_wrapper(*args, **kwargs):
                last_exc: Exception | None = None
                for attempt in range(retries + 1):
                    try:
                        return await func(*args, **kwargs)
                    except (RetryableError, *retryable) as exc:
                        last_exc = exc
                        if attempt < retries:
                            delay = delays[min(attempt, len(delays) - 1)]
                            logger.info(
                                "工具 %s 重试 %d/%d（%s）",
                                func.__name__, attempt + 1, retries, type(exc).__name__,
                            )
                            await asyncio.sleep(delay)
                logger.error("工具 %s 重试耗尽：%s", func.__name__, last_exc)
                return f"工具调用失败（{type(last_exc).__name__}）：{last_exc}。请稍后重试。"

            return async_wrapper
        else:
            @wraps(func)
            def sync_wrapper(*args, **kwargs):
                last_exc: Exception | None = None
                for attempt in range(retries + 1):
                    try:
                        return func(*args, **kwargs)
                    except (RetryableError, *retryable) as exc:
                        last_exc = exc
                        if attempt < retries:
                            delay = delays[min(attempt, len(delays) - 1)]
                            logger.info(
                                "工具 %s 重试 %d/%d（%s）",
                                func.__name__, attempt + 1, retries, type(exc).__name__,
                            )
                            time.sleep(delay)
                logger.error("工具 %s 重试耗尽：%s", func.__name__, last_exc)
                return f"工具调用失败（{type(last_exc).__name__}）：{last_exc}。请稍后重试。"

            return sync_wrapper

    return decorator
```

> **落地前最小验证（v1.1 评审要求）**：写最小用例跑一次——sync 工具（如 fetch_url）重试成功/耗尽两路径 + async 工具（模拟）不阻塞事件循环（asyncio.sleep 生效）。

### 5.3 注册表统一包装（`mcp/registry.py` 装配时，仅本地工具）

> ⚠️ 范围（v1.1）：本地注册表工具可装饰器包装；MCP 远程工具在 mcp/client 调用层统一重试（P2，不在本计划）。
> ⚠️ **幂等性假设（v1.2 实证）**：只对**读操作工具**重试（internet_search/fetch_url 幂等）。`run_skill_script` 实测脚本含写操作（`research-report/scripts/build_report.py` 写 `report_draft.md`）——重试会导致重复/覆盖写，**排除重试**；若未来脚本需重试，须脚本层保证幂等（写前检查）或单独评估。

```python
from src.core.retry import retry_tool

# 可重试的关键工具（网络类）统一包重试；本地计算类（沙箱纯代码）不包
RETRYABLE_TOOLS = {"internet_search", "fetch_url"}  # 只包读操作（幂等）；run_skill_script 排除（v1.2：脚本有写操作，重试幂等风险）

TOOL_REGISTRY = {
    name: (retry_tool()(impl) if name in RETRYABLE_TOOLS else impl)
    for name, impl in _RAW_TOOLS.items()
}
```

### 5.4 chat.py 降级矩阵核对（逐节点精确捕获，示例）

```python
# 会话落库：可重试 → 重试 1 次 → 降级内存会话
try:
    session = await repo.create_session(conn, sid)
except RetryableError:
    logger.warning("会话落库可重试失败，降级内存会话（未持久化）")
    session = session  # 降级：跳过落库，复用传入 session 对象继续（v1.2：项目无 MemorySession 类，免新建——后续消息继续尝试落库）
# 禁止：except Exception 包整段
```

---

## 6. 实施计划与评审点 🟠 v1.1

### 6.1 分批实施

| 批次 | 内容 | 产出 | 风险 |
|------|------|------|------|
| P1-a | errors.py 四分类扩展（+ category 字段，retryable 属性兼容）| 四分类 + 单测 | 低（纯增量）|
| P1-b | core/retry.py 重试装饰器 + 注册表接入 3 工具 | 重试 + 单测 | 低 |
| P1-c | chat 降级矩阵逐节点核对（裸捕获清理 + 精确降级）| 矩阵落地 + 回归 | 中（chat.py 核心）|
| P1-d | 超时统一（settings.tool_timeout_seconds）+ 评估跑数验证 + **AST 扫描脚本**（裸 except 检测，进 check_code_style 同族）| 数据 + 更新弹药库 | 中（真实 LLM 跑数）|

> **超时与重试关系（v1.1 明确）**：工具调用设超时（`tool_timeout_seconds`）→ 超时抛 `TimeoutError`/`httpx.TimeoutException` → **被重试装饰器捕获（§5.2 内置）** → 重试 → 重试也超时 → 降级返回。超时是"何时放弃单次"，重试是"几次机会"，两者串联：超时是重试的触发源之一。

### 6.2 评审点

1. 异常四分类（新增 BusinessError/SystemError + category 字段 + 边界示例 v1.1）——认可？
2. 工具重试只包 3 个网络类工具（search/fetch/skill），沙箱纯计算不重试；**本地工具范围，MCP 远程 P2**——认可？
3. 重试预算 2 次退避 0.5s/1s + extra_exceptions 参数 + httpx 内置——认可？
4. chat 降级矩阵按 §5.4 精确捕获（禁裸 except）+ AST 扫描脚本——认可？
5. **v1.1 修正确认**：重试装饰器 sync/async 双版本、会话降级行为（单次降级后续继续落库）、BusinessError 边界示例——认可？

> 📌 **学习需求**：异常分类 + 重试装饰器核心逻辑手写（02-hands-on-training：容错是工程能力核心）；矩阵核对逐节点人工确认。
