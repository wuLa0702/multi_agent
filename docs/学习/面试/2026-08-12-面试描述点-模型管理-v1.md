# 面试描述点-模型管理 v1

> 📋 **规范**：遵循 `规则/文档治理规则.md`、`规则/面试描述点规则.md`（R1-R5）
> 📌 **更新时间**：2026-08-12
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1 | 2026-08-12 | 初版：模型管理按 R1-R5 生成（厂商/模型/单价 + 密钥纪律）|

> **目录**：§1 总 ｜ §2 分 ｜ §3 总 ｜ §4 数据弹药

> **关联**（双向）：`规则/面试描述点规则.md` ｜ `架构/参考-后端接口.md` §4.2 ｜ `docs/索引.md`

---

## §1 总（30s）

> "模型管理 = **SQLite 注册表真相源** + 厂商/模型两级 API + 单价可调（F1）——密钥只经 `.env` 变量名引用，**代码里永不出现真 key**。"

## §2 分（60-90s）

- 厂商→模型列表 API（`GET /v1/providers`，前端二级下拉）
- `PATCH /v1/providers/models/{id}/price`：单价可调 + registry reload 生效（成本核算 F1）
- 密钥纪律：`api_key_env` 存变量名，运行时读 .env——**硬约束**

## §3 总（20s）

> "模型管理是真相源 + 安全底线——配置可查、单价可算、密钥不外泄；`面试描述点-成本控制` 的单价入口就在这里。"

## §4 数据弹药

| 数据 | 值 | commit |
|------|-----|--------|
| 密钥 | 只存 .env 变量名 | `b16420c` |
| 单价 API | PATCH + reload 生效 | `022dc42` |

> 📌 双向链接：契约 `架构/参考-后端接口.md` §4.2；联动 `面试描述点-成本控制` / `面试描述点-运行时模型切换`。
