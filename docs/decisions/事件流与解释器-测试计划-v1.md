# 事件流与解释器-测试计划 v1（P0/P1/P2 全量）

> 📋 **规范**：遵循 `docs/文档规范.md`
> 📌 **更新时间**：2026-08-05
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动（精确到二级标题） |
> |------|------|------|
> | v1.1 | 2026-08-05 | §1.2 环境阻塞**解除更新**（.venv312 建立，quickjs import 成功，双环境 182 绿）；§3.3 T12-T14 状态改「可执行（待统一测试）」；§4 执行记录回填环境项与双环境验证证据 |
> | v1 | 2026-08-05 | 初版：全量测试计划——§3 用例集合（P0 回归 / P1 SSE / P2 解释器 / 真实链路）；§4 执行记录表（按 §3 逐条实测回填） |

> **目录**：
> - §1 总：背景、范围与环境
> - §2 分·测试前置与 mock 策略
> - §3 分·测试用例集合（P0 回归 / P1 SSE / P2 解释器 / 真实链路）
> - §4 总·执行记录（实测回填）

> **关联文档**：
> - 引入方案：`docs/decisions/方案-EventStreaming与Interpreter引入-v1.md`（§5 测试设计 / §6 优先级）
> - 文档规范：`docs/文档规范.md`（v4：设计文档必含核心代码）

---

## 1. 总：背景、范围与环境

### 1.1 背景

引入方案 P0（事件流多事件分发）已实施完成（167 用例全绿）；本计划覆盖
P1（SSE 契约补齐——已随 P0 落地，回归验证）+ P2（Interpreters）+ 真实链路验证。

### 1.2 环境状态（2026-08-05，阻塞已解除）

| 项 | 状态 | 说明 |
|----|------|------|
| Python 3.14.5 + Windows（主环境） | ⚠️ 仍阻塞 | `bsdiff4` 无 py3.14 wheel——主环境不装 quickjs（不影响其余功能） |
| **Python 3.12 venv（backend/.venv312）** | ✅ **阻塞解除** | 方案 A（独立 venv，用户拍板）：3.12.10 建立，`deepagents[quickjs]` 安装成功，`langchain_quickjs` import 通过；`.gitignore` 已忽略（`.venv*/`） |
| 真 QuickJS 执行 | ✅ 可执行（待统一测试） | 用 `.venv312` 跑 T12-T14 |
| mock 路径测试 | ✅ 已执行 | P2 逻辑组全绿（惰性 import 降级路径验证） |
| **双环境行为一致性** | ✅ 已验证 | 3.14 主环境 182 passed + 3.12 venv 182 passed——零影响 |

### 1.3 测试范围

| 组 | 内容 | 执行方式 |
|----|------|---------|
| P0 回归 | 事件分发器 / SSE 集成 / 开关回退 | pytest 自动化 |
| P1 回归 | ToolCallEvent/SubagentEvent 契约字段 | pytest 自动化 |
| P2 逻辑 | PTC 白名单校验 / 挂载开关 / 惰性降级 | pytest 自动化（mock） |
| P2 真执行 | QuickJS eval / PTC 并行 / task() 扇出 | ⏳ 环境阻塞（用例保留待解） |
| 真实链路 | subagent 事件形态（lc_agent_name 子图链） | 浏览器/真实模型实测（待） |

---

## 2. 分·测试前置与 mock 策略

- LLM 全 mock（FakeDeepAgentModel，conftest）：不出工具调用 → 子代理不触发，
  真链路项标注待真实模型
- SDK/外部依赖全 mock（延续 test_adapter 模式）
- 事件分发测试用**合成事件流**（_FakeStreamAgent 产出指定顺序事件）——
  断言分发逻辑与时序，不依赖图执行
- quickjs 缺失时：`interpreter_enabled=True` 构建 → 明确错误提示（惰性 import 路径）

---

## 3. 分·测试用例集合

### 3.1 P0 回归组（已实施，回归验证）

| 用例 | 断言 | 状态 |
|------|------|------|
| T01 事件按实际顺序交错输出（评审问题 1） | token/工具/子代理按发生顺序，非串行分块 | ✅ 通过（28bc6e9 后） |
| T02 事件 id 自增 | 前端去重排序 | ✅ |
| T03 tool_call input 截断 ≤300 | 密钥纪律 | ✅ |
| T04 空 chunk 过滤 | 工具 chunk 天然过滤（v2 语义） | ✅ |
| T05 SSE 集成：v3 开关 → tool_call 交错在 token 流 | start→token→tool→tool→token→done | ✅ |
| T06 SSE 默认关 → 仅 start/token/done | 零行为变化 | ✅ |

### 3.2 P2 逻辑组（mock，可执行）

| 用例 | 断言 | 状态 |
|------|------|------|
| T07 `_parse_ptc_whitelist` 正常解析 | 逗号分隔 → 列表 | ⏳ 待执行 |
| T08 PTC 白名单拒绝非只读工具 | write_file/run_code_in_sandbox 等 → ValueError（fail fast） | ⏳ 待执行 |
| T09 解释器挂载开关：enabled=False → middleware 栈无 CodeInterpreter | 现状不变 | ⏳ 待执行 |
| T10 解释器挂载开关：enabled=True + quickjs 缺失 → 明确错误提示 | "bsdiff4/Python 3.11-3.12" 指引 | ⏳ 待执行 |
| T11 中间件顺序：eval 审计捕获（评审问题 2.2） | enabled=True 时 CodeInterpreter 与 ToolAudit 顺序断言（预案：ToolAudit 外层优先） | ⏳ 待执行 |

### 3.3 P2 真执行组（环境已解，待统一测试）

> 执行环境：`backend/.venv312`（Python 3.12，quickjs 已装）——v1.1 环境阻塞解除。

| 用例 | 断言 | 状态 |
|------|------|------|
| T12 QuickJS eval 工具执行 | eval 调用 → 返回结果 | ⏳ 可执行（待统一测试） |
| T13 PTC 并行搜索聚合（Promise.all 3 路） | 调用次数=3、结果聚合 | ⏳ 可执行（待统一测试） |
| T14 task() 动态子代理扇出 | N 个 item 逐个委派 reviewer | ⏳ 可执行（待统一测试） |

### 3.4 真实链路组（需真实模型/浏览器）

| 用例 | 断言 | 状态 |
|------|------|------|
| T15 subagent 事件真实形态（lc_agent_name 子图链） | 子代理执行 → subagent 事件 name/status 正确 | ⏳ 待真实链路（Fake 不出工具调用） |
| T16 打字机效果回归（浏览器） | token 逐 chunk 渲染 | ⏳ 待浏览器 |

---

## 4. 总·执行记录（实测回填）

| 用例 | 结果 | 证据 | 日期 |
|------|------|------|------|
| T01-T06（P0 回归） | ✅ 通过 | pytest 全量 179 passed；test_stream_events 5 用例 + test_chat 2 集成用例 | 2026-08-05 |
| T07 PTC 正常解析 | ✅ 通过 | test_interpreter.py::test_ptc_whitelist_parses_normal | 2026-08-05 |
| T08 非只读拒绝 | ✅ 通过 | 参数化 7 个危险工具名全拒绝（含混合白名单部分放行拒绝） | 2026-08-05 |
| T09 挂载开关默认关 | ✅ 通过 | middleware 栈无 CodeInterpreter，现状不变 | 2026-08-05 |
| T10 quickjs 缺失降级 | ✅ 通过 | RuntimeError 含 "bsdiff4" 环境指引（惰性 import 路径） | 2026-08-05 |
| T11 中间件顺序 | ✅ 通过 | ToolAudit 在 CodeInterpreter 前（外层先审计，评审问题 2.2 预案落位） | 2026-08-05 |
| T12-T14（真 QuickJS） | ⏳ 环境已解，待统一测试 | v1.1：`.venv312`（3.12）建立，quickjs import 通过，demo 脚本正常分支输出；统一测试时执行 | 2026-08-05 |
| T15 subagent 真实链路 | ⏳ 待真实模型 | Fake 不出工具调用，子代理不触发；lc_agent_name 分发逻辑已单测（合成事件），真形态待浏览器 | 2026-08-05 |
| T16 打字机浏览器回归 | ⏳ 待浏览器 | token 走 v2 chunk 流（校准后）——现状行为不变，回归由 T05/T06 覆盖 | 2026-08-05 |

**汇总**：自动化 17 项全绿（P0 7 + P2 逻辑 10）+ **双环境一致性验证 2 项**（3.14 主环境 182 + 3.12 venv 182）；真 QuickJS 3 项环境已解待统一测试；真实链路 2 项待模型/浏览器。
