# 上下文工程开发计划 v1（现状差距 → 实施路线 · 评审稿）

> 📋 **规范**：遵循 `docs/文档规范.md`
> 📌 **更新时间**：2026-08-05
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动（精确到二级标题） |
> |------|------|------|
> | v1 | 2026-08-05 | 初版：基于《上下文工程-现状分析 v1.2》差距清单的开发计划——§2 差距回顾（10 项）；§3 实施路线（三批：约定与分层 / 检索升级 / 优化类）；§4 逐项实施设计（做什么/涉及文件/验收）；§6 优先级排期；§7 核心代码 |

> **目录**：
> - §1 总：背景、目标与范围
> - §2 分·差距清单回顾（10 项）
> - §3 分·实施路线（三批）
> - §4 分·逐项实施设计（P0/P1/P2）
> - §5 分·测试设计
> - §6 总·排期与风险自检
> - §7 分·完整核心代码

> **关联文档**：
> - 现状分析：`docs/学习/上下文工程-现状分析-v1.md`（差距来源，§4 初版方案）
> - 记忆体系：`docs/decisions/方案-记忆能力开发-v3.md`（记忆检索升级依赖）
> - Skill 体系：`docs/decisions/方案-Skill体系-v1.md`（技能拆分约定）
> - 文档规范：`docs/文档规范.md`（v4：设计方案必含核心代码）

---

## 1. 总：背景、目标与范围

### 1.1 背景

《上下文工程-现状分析 v1.2》盘点出 10 项差距（已含初版方案与为什么）——
本计划将其转化为**可执行开发计划**：三批实施路线 + 逐项设计 + 核心代码。

### 1.2 目标

1. 差距 → 开发项（明确做什么/涉及文件/验收）
2. 三批排期（P0 约定 / P1 配置与分层 / P2 优化）
3. 避免重复：已覆盖项标注（如技能拆分约定 Skill 体系已做）

### 1.3 范围

| 在本计划内 | 不在本计划内 |
|-----------|-------------|
| 10 项差距的实施设计（§4） | 向量检索引入（P2 语义检索的具体引擎选型，标注待定） |
| 三批实施路线与排期 | 记忆抽取子代理（独立方案已另立文档） |
| 逐项核心代码（§7） | 前端用量告警 UI（标注后续） |

---

## 2. 分·差距清单回顾（10 项）

| # | 差距 | P 级 | 已覆盖标注 |
|---|------|------|-----------|
| 1 | 压缩阈值用官方默认 | P1 | — |
| 2 | 记忆注入无相关性/时间衰减 | P1 | 记忆方案 §5 已规划 |
| 3 | 技能全文过大无拆分约定 | P0 | ✅ Skill 体系 §4.3 已做（约定落地，本计划核对即可） |
| 4 | 上下文用量只记录不干预 | P2 | — |
| 5 | 子代理角色单一 | P2 | 隔离模板已就绪 |
| 6 | 自定义压缩器缺失 | P2 | — |
| 7 | System prompt 分层管理缺失 | P1 | — |
| 8 | 工具描述优化缺失 | P2 | — |
| 9 | 上下文窗口场景化缺失 | P2 | 多 provider 切换已就绪 |
| 10 | 大文件 offload 清理 | 闭环 | ✅ 已满足 |

---

## 3. 分·实施路线（三批）

| 批次 | 内容 | 依赖 | 工作量 |
|------|------|------|--------|
| **第一批（P0+P1 高价值低风险）** | 技能拆分约定核对（#3）+ System prompt 分层（#7）+ 压缩阈值配置化（#1） | 无 | 小 |
| **第二批（P1 中）** | 记忆检索升级（#2——关键词/标签 + 时间衰减 + 窗口 ≤10%） | 记忆方案 v3 基建（已就绪） | 中 |
| **第三批（P2 优化）** | 告警（#4）/ 子代理扩展（#5）/ 自定义压缩器（#6）/ 工具描述审计（#8）/ 窗口场景化（#9） | 前两批 | 中-大 |

---

## 4. 分·逐项实施设计

### 4.1 第一批：技能拆分约定核对（#3，P0）

- 现状：Skill 体系 §4.3 已定义"SKILL.md 拆概览+分文件"——**核对落地**：
  builtin 技能（research-report/template）是否符合约定（入口 ≤2KB）
- 涉及：backend/assets/skills/builtin/ 技能文件（核对）
- 验收：所有内置技能 SKILL.md 入口 ≤2KB，深水区在 references/

### 4.2 System prompt 分层管理（#7，P1）

- 设计：分层——**核心层**（DEFAULT_SYSTEM_PROMPT 角色，必选）/
  **可选层**（MEMORY_GUIDANCE_V3、模式指令，按需注入）/ **动态层**（记忆注入，随会话）
- 实施：prompts.py 分层常量 + main_agent 组装函数（`build_system_prompt(mode)`）；
  超限裁剪优先级：核心 > 可选 > 动态
- 涉及：`src/agent/prompts/`、`src/agent/main_agent.py`、`src/api/chat.py`
- 验收：system prompt 组件可枚举、裁剪优先级测试

### 4.3 压缩阈值配置化（#1，P1）

- 设计：config 加 `context_tool_evict_limit`（默认对齐官方 20000）+ 审计记录
  实际驱逐次数（P1 量化后再调参）
- 实施：⚠️ create_deep_agent 无直接参数（graph.py:753 固定构建）——
  学习 demo 阶段**接受默认 + 审计观察**；自定义中间件标注 P2（#6 一并做）
- 涉及：config.py（字段）+ audit（观察记录，P1 轻量）
- 验收：字段存在 + 审计观察点

### 4.4 记忆检索升级（#2，P1——第二批）

- 设计：接记忆方案 §5——关键词/标签过滤 + 时间衰减（新记忆权重大）+
  注入窗口 ≤10% 裁剪（优先级：画像 > 任务 > 事实 > 旧决策）
- 实施：`load_recent_memories_v3` 升级（排序加时间权重 + 上限按窗口比例）
- 涉及：`src/agent/memory/store.py`、`src/api/chat.py`
- 验收：注入条数按窗口动态 + 时间衰减排序测试

### 4.5 第三批（P2 优化项）——设计要点

| 项 | 设计要点 | 验收 |
|----|---------|------|
| #4 用量告警 | TokenUsageMiddleware 扩展：context_used >80% → SSE 告警事件（前端提示"为什么变模糊"——与 summarizer 互补） | 告警事件测试 |
| #5 子代理扩展 | 新角色（code_agent/data_agent）复用隔离模板（CompiledSubAgent）——基建已就绪，扩展时核对 | 角色构建测试 |
| #6 自定义压缩器 | 小模型 summarizer（省主模型 token）+ 驱逐优先级 + 压缩质量评估 | 自定义中间件单测 |
| #8 工具描述审计 | 统计全部工具描述 token → 长描述拆"摘要+详情"（同 Skill 渐进披露思路） | 审计脚本输出 |
| #9 窗口场景化 | ChatContext 扩展 context_profile（small/standard/large）→ 模型选择联动 | 切换测试 |

---

## 5. 分·测试设计

| 批 | 用例 | 断言 |
|----|------|------|
| 一批 | 内置技能入口 ≤2KB 核对 | 文件大小断言 |
| | build_system_prompt 分层组装（核心/可选/动态） | 组件枚举 |
| | 裁剪优先级（核心 > 可选 > 动态） | 裁剪断言 |
| | context_tool_evict_limit 字段存在 | 配置断言 |
| 二批 | 记忆注入时间衰减排序（新记忆在前） | 排序断言 |
| | 注入窗口 ≤10% 动态裁剪 | 条数断言 |
| 三批 | 告警事件（>80% 触发） | SSE 事件断言 |
| | 工具描述 token 审计输出 | 统计断言 |
| 兼容 | 现有 215 用例全绿（回归） | 回归 |

---

## 6. 总·排期与风险自检

### 6.1 排期建议

| 批次 | 建议时机 | 备注 |
|------|---------|------|
| 第一批 | 可立即（小改动） | #3 核对 + #7 分层 + #1 字段/审计 |
| 第二批 | 记忆体系稳定后 | #2 依赖记忆方案已落地基建 |
| 第三批 | 学习 demo 按需 | 优化类，不阻塞 |

### 6.2 风险自检清单

- [ ] **#1 阈值调参依赖数据**：审计观察先行（P1 只加字段+审计，不盲调阈值）
- [ ] **#7 分层不破坏现有 prompt**：核心层内容零改动（只重组装结构）——回归保障
- [ ] **#2 时间衰减权重**：初始简单权重（如 ts 线性衰减），P2 再精细
- [ ] **#6 自定义中间件替换风险**：deepagents 内部 FilesystemMiddleware 是必需
      中间件——替换需保 _permissions 传递（评审点）
- [ ] **#9 模型选择联动**：context_profile 映射到 registry 模型——小模型需存在
- [ ] **先红后绿**：每批测试先行；提交前 pytest 全绿

---

## 7. 分·完整核心代码（文档规范 v4）

### 7.1 System prompt 分层（#7，第一批）

```python
"""（src/agent/prompts/prompts.py 增量 + main_agent 组装）"""

# 分层常量（v1 计划 §4.2）：
PROMPT_LAYERS = {
    "core": ["DEFAULT_SYSTEM_PROMPT"],              # 核心层：角色（必选）
    "optional": ["MEMORY_GUIDANCE_V3", "MODE_INSTRUCTIONS"],  # 可选层：按需注入
    "dynamic": ["memory_injection"],                # 动态层：记忆注入（chat.py）
}
# 裁剪优先级：核心 > 可选 > 动态


def build_system_prompt(*, mode: str = "default") -> str:
    """分层组装 system prompt（核心 + 可选按需 + 动态由 chat 注入）。

    Args:
        mode: 代理模式（plan/agent/auto → 追加可选层模式指令）

    Returns:
        组装后的 system prompt 字符串
    """
    from src.agent.prompts import DEFAULT_SYSTEM_PROMPT, MEMORY_GUIDANCE_V3, MODE_INSTRUCTIONS

    parts = [DEFAULT_SYSTEM_PROMPT, MEMORY_GUIDANCE_V3]
    instruction = MODE_INSTRUCTIONS.get(mode)
    if instruction:
        parts.append(instruction)
    return "\n".join(parts)
```

### 7.2 压缩阈值配置 + 审计观察（#1，第一批）

```python
"""（config.py 增量 + 审计观察点）"""

    # ── 上下文工程（2026-08-05 开发计划）──
    # 工具结果驱逐阈值（默认对齐官方 20000；P1 只观察不盲调）
    context_tool_evict_limit: int = 20000
```

```python
# 审计观察（P1 轻量）：记录驱逐触发——deepagents 无直接钩子，
# 通过日志观察（tool result offload 路径有 deepagents 内部日志）；
# 自定义中间件计数留 P2（#6 一并做）
```

### 7.3 记忆检索升级（#2，第二批）

```python
"""（memory/store.py load_recent_memories_v3 升级：时间衰减 + 窗口比例）"""

async def load_recent_memories_v3(
    store, limit: int = MEMORY_INJECT_LIMIT, *, window_ratio: float = 0.1
) -> list[str]:
    """记忆注入（v1 计划 §4.4）：时间衰减排序 + 窗口 ≤10% 动态裁剪。

    Args:
        store: AsyncSqliteStore
        limit: 基础条数上限
        window_ratio: 注入占上下文比例上限（0.1 = 10%）

    Returns:
        记忆内容列表（新→旧，画像优先）
    """
    ...  # 现有合并逻辑 + 排序改：画像优先 → 时间衰减（ts 线性权重）
    # 窗口裁剪：估算注入 token ≤ window_ratio × 窗口（128k）——按比例截断
```

### 7.4 用量告警（#4，第三批）

```python
"""（TokenUsageMiddleware 增量：接近上限告警）"""

CONTEXT_WARN_RATIO = 0.8   # context_used > 80% 窗口 → 告警标记

# done 事件扩展：context_used / context_total 已知——
# 新增 context_warning: bool（>80% 时 True，前端提示"为什么变模糊"）
# （与 summarizer 互补：压缩=自动兜底，告警=告知原因）
```

### 7.5 工具描述审计脚本（#8，第三批）

```python
"""（scripts/audit_tool_descriptions.py：工具描述 token 审计）"""

def audit_tool_descriptions() -> dict:
    """统计全部注册工具描述长度（token 估算：字符数/4）。

    Returns:
        {"tool_name": {"desc_len": N, "chars": M}, "total_chars": X}
    """
    from src.mcp.registry import TOOL_REGISTRY

    result = {}
    for name, tool in TOOL_REGISTRY.items():
        desc = getattr(tool, "description", "")
        result[name] = {"desc_len": len(desc) // 4, "chars": len(desc)}
    result["total_chars"] = sum(v["chars"] for v in result.values() if isinstance(v, dict))
    return result
```
