# 2026-08-10 评估定位任务跑偏——工具审计分析法（面试素材）

> 📋 **规范**：遵循 `docs/规则/文档规范.md`
> 📌 **更新时间**：2026-08-10
> 📝 **版本变更记录**（永久保存，只追加不删除）：
> | 版本 | 日期 | 具体改动 |
> |------|------|---------|
> | v1.2 | 2026-08-11 | 追加 §trace 三层根因定位（阶段二）：爬虫循环 → 写文件循环 → 结论（prompt 约束无法根治，需环境层沙箱断网） |
> | v1.1 | 2026-08-10 | 追加 §验证结果：修复生效（97 次搜索全走子代理委派）+ 审计盲区发现（子代理内部工具调用不打点）——"0 次搜索"是假象，bocha 请求计数才是真相 |
> | v1 | 2026-08-10 | 初版：场景 / 发现 / 根因 / 解决方法 / 教训与面试价值 |

---

## 场景（2026-08-10，评估体系 P0-2 验证）

P0-2 审核子代理上线后跑 3 任务小样本评估（task-001/003/007），墙钟 **24 分钟**，远超 P0-1 同任务耗时（2-8×）。co-creator 追问"耗时在哪"，我最初凭直觉断言"研究任务多轮搜索导致"——**该判断被审计日志推翻**。

## 发现（数据说话，推翻直觉）

用 `logs/file_access_audit.jsonl`（工具审计全量 JSONL，2216 条）按评估窗口时间戳（20:19-20:38）+ `thread_id` 过滤重建工具调用序列：

| 工具 | 次数 | 含义 |
|------|------|------|
| `run_command_in_sandbox` | **82** | 沙箱命令执行（单次 5-30s）|
| `edit_file` | **26** | 沙箱内编辑文件 |
| `task`（子代理委派）| 24 | search/review 子代理 |
| `run_code_in_sandbox` | 16 | 沙箱代码执行 |
| **`internet_search`** | **仅 3** | 联网搜索（研究任务本该是主力）|

**真相**：研究任务**跑偏**——该联网搜索（3 次）却疯狂操作沙箱（124 次）。50 轮 recursion 撞限不是"搜索循环"，是**沙箱操作循环**。

## 根因

任务 query **未约束工具偏好**：只要求"调研产出研报"，模型自由发挥时倾向"写代码处理数据"路径（沙箱可执行 Python/命令），而非"联网搜索"路径。

## 解决方法（2026-08-10 落地）

`backend/assets/eval/tasks.jsonl` 全部 10 个任务 query 统一追加约束句：

> "以联网搜索（internet_search）为主要信息源，沙箱仅用于必要的数据验证。"

（保持"引用编号要求"不变——A1 指标前提不破坏。）

## 教训（两层）

1. **先看数据再下结论**：我第一反应"多轮搜索导致慢"是直觉猜测，审计日志 30 秒推翻——工具审计中间件（只记不拦）的设计价值在此体现
2. **prompt 不约束 = 模型自由发挥**：任务型 prompt 必须显式声明"用什么工具为主"，否则模型倾向选择执行路径（沙箱）而非调研路径（搜索）

## 面试价值（bad case 讲法）

> "评估跑数发现研究任务跑偏：该搜索不搜、疯狂调沙箱（82 次命令 vs 3 次搜索）——用工具审计 JSONL 按时间窗口定位，根因是任务 prompt 未约束工具偏好，修正后重跑对比。**慢的真相要靠数据，不靠直觉**。"

## 验证结果（2026-08-10，修复后重跑同 3 任务 · run 98fce243）

| 指标 | 修复前（8a3b0b19） | 修复后（98fce243） | 解读 |
|------|--------------------|--------------------|------|
| 博查搜索（bocha 请求） | 81 | **97** | 搜索量不降反升 |
| 主 agent 直调搜索（审计可见） | 3 | **0** | 搜索全部委派 search_agent |
| 子代理委派（task） | 24 | 27 | 委派路径为主 |
| task-007 质量 | 完整度 3.0 / 可靠性 3.0 | **4.0 / 3.0（历史最佳）** | 约束 + 审核回路组合最优 |
| 完成率 | 2/3 | 1/3（task-001/003 撞 recursion 50） | ⚠️ 撞限根因仍不明 |

**关键新发现——审计盲区**：`ToolAuditMiddleware` 只审计**主 agent 层**工具调用，**search_agent 子代理内部的工具调用不打 audit 点**（修复后主 agent 直调搜索为 0，但 bocha 实际请求 97 次——全部发生在子代理内部，审计不可见）。"0 次搜索"是假象，**bocha 外部请求计数才是真相**。

**教训追加**：审计口径要讲清楚"覆盖哪一层"——多层 Agent 架构下，子代理内部调用需要独立打点或外部服务计数交叉验证（这正是推理层 trace 采集的动机）。

## trace 三层根因定位（2026-08-11，阶段二） 🔴 v1.2

**工具**：推理层 trace 采集（阶段一，logs/agent_trace.jsonl——on_chat_model_start 摘要/工具输入输出/子代理启停，parent_ids 区分层级）。task-001 单任务逐轮修复，三轮定位：

| 轮次 | trace 证据 | 根因 | 修复 | 效果 |
|------|-----------|------|------|------|
| 1 | 撞限前 45 事件全为 run_command/run_code | **模型绕过搜索，在沙箱自写爬虫**（curl/urllib 抓官网 + Python 解析 HTML） | query 加"禁止在沙箱内联网抓取" | 爬虫大减（66→12 次），但模型**仍坚持抓**（16 次 urllib） |
| 2 | 审核子代理跑通后，反复 edit_file 编辑 /workspace/*.md | **交付物形式偏差**：模型把"交付研报"理解为写沙箱文件反复打磨，不输出文本 | query 加"最终回复直接输出完整研报正文" | 不再写文件，但**仍爬虫循环** |
| 3 | internet_search 仅 1 次 + 16 次 urllib 抓取（含 ssl context 调试输出） | **提示词约束 vs 模型动机冲突**：博查摘要满足不了"价格/参数量"精确需求 → 模型选择"更可靠"的官网抓取——prompt 禁令约束不住 | 结构性结论（见下） | — |

**核心结论**：prompt 层约束**无法根治**模型自写爬虫——信息获取的**动机驱动**（对搜索结果不满足时自行抓取）压过指令。根治需**环境层硬约束：沙箱断网**（opensandbox `[egress]` 代理已配置未启用白名单——需查官方规则格式验证）。这也是**生产安全议题**：沙箱内任意代码可联网 = 数据外泄风险，Agent 信息获取入口应收敛到受控工具（internet_search 有审计/白名单）。

**面试价值（完整 bad case 故事）**：
> "评估跑数暴露模型绕过搜索工具、在沙箱自写爬虫循环 50 轮——trace 逐层定位（工具循环→爬虫→写文件→动机冲突），prompt 约束部分有效但根治需环境层（沙箱断网 + 信息入口收敛到受控工具）。**提示词管不住的行为，要用环境约束**。"

## 关联

- 审计实现：`src/agent/middlewares/tool_audit.py`（audit logger → `logs/file_access_audit.jsonl`）
- 推理层 trace：`core/logging.py` trace logger → `logs/agent_trace.jsonl`（阶段一 d04cd79/011a8a6）
- 评估报告：`data/eval/reports/98fce243`（修复验证）、`8a3b0b19`（P0-2 样本）、`b424e230`（P0-1 基线）
- 任务集：`backend/assets/eval/tasks.jsonl`（四约束：搜索为主/禁沙箱抓取/引用编号/直接输出）
