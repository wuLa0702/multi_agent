# 方案：后端基础架构（FastAPI 跑起来 + Redis + SQLite）

> 状态：**待评审**（v1 评审稿，co-creator 评审后实施）
> 目标：让后端**最小可跑**——FastAPI 启动 + 连 Redis + 连 SQLite，可验证、可测试、可上云。

## 1. 目标与范围

| 做 | 不做（v3 再做） |
|----|----------------|
| FastAPI 入口 + 健康检查 | Agent 编排 / deepagents |
| Redis 连接（短期记忆地基） | SSE 流式 / chat 接口 |
| SQLite 建表 + DAO（长期记忆地基） | MCP 网关 / 沙箱接入 |
| 分层测试（redis mock + sqlite :memory:） | 前端 |

**验收标准**：`curl /v1/health` 返回 `{status: ok, redis: connected, sqlite: connected}`；`pytest` 全绿。

## 2. 目录落点（新增文件）

```
backend/src/
├── api/
│   ├── main.py            # FastAPI 入口（CORS、路由注册、health）
│   └── health.py          # GET /v1/health（redis ping + sqlite 探活）
├── core/
│   ├── redis.py           # ★新增 Redis 连接池（redis.asyncio，lifespan 管理）
│   ├── db.py              # ★新增 SQLite 连接（aiosqlite + schema 初始化）
│   └── errors.py          # 异常分级（先建骨架）
├── db/
│   ├── schema.py          # ★建表 SQL（sessions / messages）
│   └── repository.py      # ★DAO：session CRUD + message 追加
└── models/
    ├── session.py         # Session 实体（Pydantic）
    └── message.py         # Message 实体（Pydantic）
```

## 3. 核心设计决策

### 3.1 Redis：连接池 + lifespan
- 用 `redis.asyncio` 客户端，**进程级单例连接池**（`get_redis()` + `close_redis()`）
- FastAPI `lifespan` 启动时连接、关闭时释放
- 短期记忆语义：`sessions:{id}` hash 存会话元数据 + `messages:{id}` list 存消息流
- 连接失败 → health 显示 `redis: disconnected`（**不崩溃**，服务照常起）——本地没 redis 也能开发

### 3.2 SQLite：schema 先行 + DAO 参数化
- `db/schema.py` 集中 SQL（符合旧项目规范「所有 SQL 写在 schema.py，DAO 只调用」📌）
- 建表：`sessions(id, title, created_at, updated_at)` + `messages(id, session_id, role, content, created_at)`
- `aiosqlite` 异步访问，`:memory:` 测试注入
- 路径用 `core/paths.get_db_path()`（dev 本地 / prod 云端自动切换 ✅ 已有）

### 3.3 健康检查 = 验收点
```json
GET /v1/health → {
  "status": "ok",
  "redis": "connected" | "disconnected",
  "sqlite": "connected",
  "env": "dev"
}
```
- 不依赖 LLM/沙箱——**只验证地基**，跟 Agent 无关的纯基建层

### 3.4 依赖
- 新增：`redis>=5.0`（已有 ✅）、`aiosqlite>=0.20`（已有 ✅）——**零新增依赖**

## 4. 启动与验证

```bash
# 1. 起 Redis（本地 docker，或直接连云端——.env.dev 的 REDIS_URL 决定）
docker compose --env-file .env.dev up -d redis

# 2. 启动后端
cd backend && uvicorn src.api.main:app --reload --port 8000

# 3. 验证
curl http://localhost:8000/v1/health
# → {"status":"ok","redis":"connected","sqlite":"connected","env":"dev"}

# 4. 测试
cd .. && pytest backend/tests -v   # 全绿
```

## 5. 测试策略（对应 20-testing.md）

| 测试 | 位置 | Mock 方式 |
|------|------|-----------|
| health 返回 ok（redis+sqlite 均通） | test_api/test_health.py | mock redis ping + :memory: sqlite |
| redis 断开时 health 不崩溃 | test_api/test_health.py | mock ping 抛异常 → disconnected |
| session CRUD / message 追加 | test_core/test_repository.py | :memory: sqlite |
| 路径自适应（dev 本地/prod 云端） | test_core/test_smoke.py | 已有 ✅ |

## 6. 风险与开放问题

| 风险 | 缓解 |
|------|------|
| 本地没 Redis 时开发受阻 | health 降级为 disconnected，服务不崩；本地可 docker 起 |
| SQLite 并发写 | 单写连接 + WAL 模式（schema 里 PRAGMA） |
| Windows 路径 | paths.py 已处理（win32 短路）✅ |

**开放问题（评审时定）**：
1. Redis 先存什么？——建议：只建连接 + 会话元数据占位，业务数据结构 v3 定
2. 消息表要不要分表（session 维度）？——建议：单表 + session_id 索引，够用再拆
3. 是否要 `/v1/sessions` CRUD 接口？——建议：要，它是前端会话列表的地基

## 7. 交付物清单
- [ ] api/main.py + health.py（health 接口）
- [ ] core/redis.py + core/db.py（连接池 + lifespan）
- [ ] db/schema.py + repository.py（建表 + DAO）
- [ ] models/session.py + message.py（Pydantic 实体）
- [ ] test_api/test_health.py + test_core/test_repository.py
- [ ] 启动验证截图/输出（health JSON + pytest 绿）

---
> 评审通过后实施；本文件归档 docs/ 并登记 learnings。
