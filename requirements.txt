# 与 pyproject.toml dependencies 保持一致（部署/CI 便捷入口）
# 安装：pip install -r requirements.txt

# Web 层
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.0

# Agent 层
langgraph>=0.2
# Checkpointer 断点持久化（P0，2026-08-04 版本锁定——计划文档 C.4-1）
langgraph-checkpoint==4.1.1
langgraph-checkpoint-sqlite==3.1.0
langchain>=0.3
langchain-openai>=0.2
deepagents>=0.5   # 主 harness；更正：PyPI 包名是 deepagents（langchain-deepagents 不存在）

# MCP 网关层
fastmcp>=2.0
langchain-mcp-adapters>=0.3.1   # MCP client：MultiServerMCPClient 连接外部 server

# 沙箱层（云端 OpenSandbox）
opensandbox>=0.1

# 记忆/存储
redis>=5.0
aiosqlite>=0.20

# 基建
pydantic>=2.8
pydantic-settings>=2.4
httpx>=0.27
pyyaml>=6.0

# 已移除（2026-08-05 最小集审计）：
# - langsmith：src 无直接 import（tracing 配置由 langchain 传递依赖提供）
# - python-dotenv：src 无直接 import（pydantic-settings 传递依赖处理 .env）
